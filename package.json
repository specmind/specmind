{
  "name": "specmind-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Architecture that evolves with your code",
  "repository": {
    "type": "git",
    "url": "https://github.com/specmind/specmind.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "validate-fixtures": "pnpm --filter=@specmind/format validate-fixtures",
    "version": "pnpm -r exec npm version --no-git-tag-version"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
